import { LightningElement } from 'lwc';

export default class Ldservice3 extends LightningElement {
    recordId

    handleSubmit(event) {
        alert('Successfully filled all fields on click of Submit');

    }
}